# MaizeCT-Pheno — ONNX build requirements (verified working set)
# Python 3.10 (Windows x64)
# Install:  pip install -r requirements-onnx.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# Inference backend: pick ONE per environment — both provide the `onnxruntime` module.
#   onnxruntime-directml -> any DirectX 12 GPU (NVIDIA / AMD / Intel), no CUDA needed
#   onnxruntime-gpu      -> NVIDIA + CUDA toolkit (fastest)
# They cannot be installed side by side.

numpy==2.2.6
scipy==1.15.3
opencv-python==5.0.0.93
pillow==12.3.0
tqdm==4.70.0
scikit-image==0.25.2
networkx
imageio
tifffile
lazy-loader

simpleitk==2.5.6
connected-components-3d==4.1.0
edt==3.1.2

# --- inference backend (choose one) ---
onnxruntime-directml==1.20.1
# onnxruntime-gpu

coloredlogs
humanfriendly
pyreadline3
flatbuffers
protobuf
sympy
mpmath

# --- GUI ---
PyQt5==5.15.11
PyQt5-Qt5==5.15.2
PyQt5-sip

# --- optional: only needed to rebuild the PyInstaller package ---
pyinstaller
pyinstaller-hooks-contrib
